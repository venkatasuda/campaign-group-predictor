# Core dependencies - everything the project NEEDS to train, serve and test.
#
# Deliberately expressed as ranges rather than exact pins: exact pins to a single
# patch release break on any Python version that predates or postdates the wheels
# for that release, and force a source build that needs a C++ toolchain. Ranges let
# pip pick a prebuilt wheel for whatever interpreter is in use.
#
# For an exact, reproducible environment, generate a lockfile once this installs:
#     pip freeze > requirements.lock.txt

# ---- Data / ML ----
pandas>=2.1,<3
numpy>=1.26,<3
scipy>=1.11
scikit-learn>=1.4,<2
joblib>=1.3

# ---- Plotting / notebook ----
matplotlib>=3.8
seaborn>=0.13
jupyterlab>=4.0
ipykernel>=6.29
ipywidgets>=8.1

# ---- Backend ----
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6,<3
pydantic-settings>=2.2

# ---- Frontend ----
streamlit>=1.32
requests>=2.31

# ---- Testing / quality ----
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
black>=24.0
ruff>=0.5
